//const { mdLinks } = require('./index.js');

/* const ruta = process.argv[2];
mdLinks(ruta)
  .then((links) => {
    console.log(links);
  })
  .catch(console.error);

 mdLinks(ruta, { validate: true })
  .then((links) => {
    console.log(links);
  })
  .catch(console.error); */

/* mdLinks('asdasd')
  .then((links) => {
    console.log(links);
  })
  .catch(console.error); */

  function sum(a,b) {
    return a + b;
  }
module.exports = sum;
